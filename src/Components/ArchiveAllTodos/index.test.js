import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom';
import Todolist from './index.js';


test('it should archive todos when they are selected', () => {

});
